//! Executor Engine orchestrator.
//! TODO: Wire NATS subscription for ScoredSignal, apply Kelly + risk + throttle, emit order intents.

pub struct ExecutorEngine;
impl ExecutorEngine { pub fn new() -> Self { Self } }

