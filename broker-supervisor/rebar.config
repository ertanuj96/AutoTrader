{erl_opts, [debug_info]}.
{deps, [
    {nats, "0.8.0"},
    {jsx, "3.1.0"}
]}.

{shell, [
    {apps, [broker_supervisor]}
]}.

{relx, [
    {release, {broker_supervisor, "0.1.0"}, [broker_supervisor]},
    {dev_mode, true},
    {include_erts, false}
]}.
